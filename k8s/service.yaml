apiVersion: v1
kind: Service

metadata:
  name: bmi-api
  labels:
    app: bmi-api

spec:
  type: ClusterIP

  selector:
    app: bmi-api

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http